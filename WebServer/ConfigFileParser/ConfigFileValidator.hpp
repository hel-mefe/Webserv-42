# include "../../Includes/Header.hpp"

class ConfigFileValidator
{
    private:

    public:
        ConfigFileValidator();
        ConfigFileValidator(const ConfigFileValidator& c);
        ConfigFileValidator& operator=(const ConfigFileValidator& c);
        ~ConfigFileValidator();
} ;